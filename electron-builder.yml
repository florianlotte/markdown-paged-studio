appId: dev.florianlotte.markdown-paged-studio
productName: Markdown Paged Studio
directories:
  buildResources: electron/icons
  output: release
# Everything the app needs at runtime is in dist/ (Vite bundles the dependencies) and electron/.
files:
  - dist/**
  - electron/**
  - package.json
  - '!node_modules/**'
artifactName: ${name}-${version}-${os}-${arch}.${ext}
npmRebuild: false
win:
  # One portable exe per architecture: markdown-paged-studio-<version>-win-x64.exe and -win-arm64.exe
  target:
    - target: portable
      arch:
        - x64
        - arm64
  icon: electron/icons/icon.png
portable:
  # One exe per architecture only; skip the combined x64+arm64 exe (twice the size, nothing more).
  buildUniversalInstaller: false
linux:
  target: AppImage
  category: Office
  icon: electron/icons/icon.png
mac:
  target:
    - dmg
    - zip
  category: public.app-category.productivity
  icon: electron/icons/icon.png
